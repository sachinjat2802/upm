# Claude Code Instructions for CPM

This repository uses CPM (Cross-language Package Manager).

## Key Commands
- Build: `cargo build`
- Test: `cargo test`
- Diagnostics: `cpm doctor`
- Cross-Language RPC: `cpm bridge call <target> '<args_json>'`
