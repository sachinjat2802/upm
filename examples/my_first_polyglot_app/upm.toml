ecosystems = [
    "uv",
    "maven",
    "pnpm",
    "cargo",
]

[project]
name = "migrated-app"
version = "0.1.0"
primary_language = "python"

[foreign_dependencies]
"cargo:serde" = "^1.0"
"npm:express" = "^4.19.2"

[transports]
default_tier = "rpc"
