[package]
name = "cpm-sdk"
version = "0.1.0"
edition = "2021"
description = "CPM Rust Client SDK for Cross-Language RPC Bridge"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
